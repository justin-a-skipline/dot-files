" The configuration itself lives in ~/.vim/vimrc, shared with vim. Anything that
" genuinely differs between the two editors is guarded there with has('nvim'),
" including the runtimepath additions that let neovim find ~/.vim.
source ~/.vim/vimrc
